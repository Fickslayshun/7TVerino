name: Release Build

on:
  push:
    branches:
      - main
  workflow_dispatch:

permissions:
  contents: write

jobs:
  release:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Node
        uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: yarn

      - name: Install dependencies
        run: yarn install --frozen-lockfile

      - name: Build production extension
        run: yarn build:prod

      - name: Build Firefox release
        run: yarn build:firefox:prod

      - name: Package release zips
        shell: pwsh
        run: |
          ./script/package-release.ps1 -SourceDir dist -PackageName 7tverino -StageDirName 7tverino
          ./script/package-release.ps1 -SourceDir dist/firefox -PackageName 7tverino-firefox -StageDirName 7tverino-firefox

      - name: Upload workflow artifact
        uses: actions/upload-artifact@v4
        with:
          name: 7tverino-release-builds
          path: |
            release-builds/7tverino.zip
            release-builds/7tverino-firefox.zip

      - name: Prepare release notes
        shell: pwsh
        run: |
          $repo = "${{ github.repository }}"
          $patchnotesUrl = "https://github.com/$repo/blob/main/patchnotes.txt"
          $updated = Get-Date -Format "dd/MM/yyyy"
          @"
          If you want to view the patchnotes, you can here: [patchnotes.txt]($patchnotesUrl)
          
          Last updated [$updated]
          "@ | Set-Content "./release-builds/release-notes.txt"

      - name: Recreate rolling release
        shell: pwsh
        env:
          GH_TOKEN: ${{ github.token }}
        run: |
          $repo = "${{ github.repository }}"
          $stableTag = "7tverino"
          $releases = gh release list --repo $repo --limit 100 --json tagName | ConvertFrom-Json

          foreach ($release in $releases) {
            if ($release.tagName -ne $stableTag) {
              gh release delete $release.tagName --repo $repo --cleanup-tag -y
            }
          }

          gh release view $stableTag --repo $repo *> $null
          if ($LASTEXITCODE -eq 0) {
            gh release delete $stableTag --repo $repo --cleanup-tag -y
          }

          gh release create $stableTag "release-builds/7tverino.zip" "release-builds/7tverino-firefox.zip" `
            --repo $repo `
            --title "7tverino" `
            --notes-file "./release-builds/release-notes.txt" `
            --latest
